# mac OS
# alias vf='fzf --preview "bat --style=numbers --color=always {}" --margin=10,14,10,14 --border=rounded | xargs -o -n 1 vim'

# ubuntu
# alias vf='fzf --preview "batcat --style=numbers --color=always {}" --margin=10,14,10,14 --border=rounded | xargs -o -n 1 vim'

# Gruvbox on mac
export CLICOLOR=1
