# dotfiles

### .vimrc

### copy to the root dir and create a symbolic link
```
ln -s ~/dotfiles/.vimrc ~/
```

### .bashrc

### copy to the root dir and create a symbolic link
```
ln -s ~/dotfiles/.bashrc ~/
```
